{
	"name": "<%= moduleName %>",
	"version": "0.0.0",
	"description": "<%= moduleDescription %>",
	"license": "MIT",
	"repository": "<%= githubUsername %>/<%= repoName %>",
	"author": {
		"name": "<%= name %>",
		"email": "<%= email %>",
		"url": "<%= humanizedWebsite %>"
	},
	"engines": {
		"node": ">=4"
	},
	"scripts": {
		"build": "browserify lib/index.js > example/bundle.js",
		"start": "browser-sync start --server 'example' --files 'example'",
		"test": "xo<% if (ava) { %>&& ava<% } %>"
	},
	"main": "lib/index.js",
	"files": [
		"lib"
	],
	"keywords": [
		"d3",
		"d3.js",
		"chart"
	],
	"dependencies": {
		"d3": "^4.3.0"
	},
	"devDependencies": {<% if (ava) { %>
		"ava": "^0.15.2",<% } %>
		"browser-sync": "^2.17.5",
		"browserify": "^13.1.1",
		"xo": "^0.16.0"
	},
	"xo": {
		"esnext": true
	}
}
